---
name: mindtodoc
description: 把粗糙的移动游戏想法（脑图、框架文档、参考游戏名）转化为高度专业、可直接开发的 md 文档。读取 input/ 素材，逐系统确认细节并补充流程图与交互原型，输出到 output/。Use when 用户要把游戏创意整理成开发文档、生成游戏开发文档、game design doc。
---

# MindToDoc

把 `input/` 下的粗糙游戏想法，转化为 `output/<游戏名>-开发文档.md` ——
一份高度专业、可直接用于开发的文档。

## 产出位置

- 主文档：`output/<游戏名>-开发文档.md`
- 界面原型：`output/prototypes/<系统英文名>.html`
- 原型图标注与规则说明：优先放在原图同目录，例如
  `input/<素材目录>/标注/` 与 `input/<素材目录>/图片内容规则说明.md`
- 文档结构来自 `templates/doc-structure.md` 与 `templates/system-module.md`，
  这两个模板是专业结构的唯一真相来源，按需修改它们而非硬编码结构。

## 流程

### 阶段 0 · 读取输入
扫描 `input/`，读取脑图、框架文档、参考游戏名等所有素材并消化。
若 `input/` 为空：提示用户先放入素材，停止，不硬跑。

### 阶段 1 · 确认整体框架
向用户汇报从输入中理解到的：游戏定位、核心玩法、目标平台，以及拆出的
**系统模块清单**及优先级。每个模块可附一行**预估交付物**（规则 / 红点 / 数据兼容 / 流程图 / 原型 / 数据表），
便于提前对齐哪些模块会走轻量路径。列出任何"待确认假设"（残缺/矛盾/仅有参考游戏名处），
由用户拍板，不擅自编造。用户确认系统清单后，按 `doc-structure.md` 产出第 1、2 章。
**线上项目**须在阶段 1 同步梳理第 2 章「数据兼容总则」（见下文），供各模块引用；
非线上项目可写「本项目不涉及线上数据兼容」并跳过。

### 阶段 2 · 逐系统深入
对系统清单中每个系统，按顺序循环：

1. **确认规则细节** —— 追问该系统的空白点，直到规则清晰（**必选**）。
   其中两项为**必考虑项**（不默认「无」，省略须在判定中显式说明）：
   - **红点规则**：状态变更待处理、奖励可领取、次数/额度已重置、新内容解锁、
     活动开启或即将结束、页签/子入口有待办等场景，按需写 `红点规则` 小节。
   - **数据兼容**（线上项目）：本模块数据是否受版本差、转服、退盟/加盟、热更、
     跨服等场景影响；有影响则写 `数据兼容` 小节，无影响写「本模块不涉及数据兼容」。
   纯后台、无玩家可感知入口的模块，红点与兼容通常可省略，但仍须显式判定。
2. **判定本模块交付物** —— 规则确认后、动笔前，根据模块特点决定还需要哪些产出；
   向用户简要说明判定理由，有异议时以用户为准。默认判定参考：

   | 交付物 | 通常需要 | 通常可跳过 |
   |--------|----------|------------|
   | 流程图 | 多状态流转、分支决策、时序/依赖复杂 | 单条开关规则、纯展示文案、逻辑一两句能说清 |
   | 界面原型 | 有专属 UI、多屏交互、布局需对齐 | 纯后台/服务逻辑、无界面、仅复用通用弹窗 |
   | 数据结构/配置表 | 需独立配表、持久化字段、跨系统实体 | 纯客户端展示、完全复用已有表、无数值配置 |

3. **按判定逐项产出** —— 只生成已判定的交付物，不为了凑齐四件套硬写：
   - 流程图 → 见「外部技能与降级」
   - 界面原型 → 见「外部技能与降级」
   - 数据结构/配置表 → 实体、字段、类型、取值范围。
     **表格单元格内禁止出现 `[]`**（钉钉文档会因此中断表格解析）：数组类型写成 `int 数组`/`enum 数组` 等，取值示例去掉方括号改用文字描述。
   - 跳过的交付物：**不写对应小节**，不在 Checklist 中勾选该项
   - 红点规则 → 需要时写入 `红点规则` 小节；不需要时不写该小节
   - 数据兼容 → 需要时写入 `数据兼容` 小节；不需要时不写该小节

4. 用户确认 → 按 `system-module.md` 把该模块追加进主文档 → 下一个系统

单个系统出错或返工不影响其他系统。

### 阶段 3 · 收尾
生成第 4 章 Checklist（按系统逐项），输出完整主文档。

## 外部技能与降级（软依赖）

- **流程图**：手写 Mermaid 代码块，并用 `npx -y @mermaid-js/mermaid-cli -i x.mmd -o x.svg`
  逐张渲染校验通过（EXIT=0）再写入文档，避免盲写语法错误；校验后删除临时文件。
  若 mmdc 不可用（无 Node/网络），降级为仅手写并留一行提示「mermaid 未校验，已手写」。
- **界面原型**：优先调用 `ui-ux-pro-max` 生成 HTML 原型存入 `output/prototypes/`；
  未装则手写 HTML/线框示意，并留一行提示。

核心流程不依赖第三方技能，任何情况下都能产出文档。

## 红点规则（手游通用）

逐模块确认规则时，按下面要点组织（按需取用，无关项不写）：

| 要点 | 说明 |
|------|------|
| 触发条件 | 什么状态变化导致亮红点（可领、可买、已重置、新解锁等） |
| 展示位置 | 红点挂在哪条入口链路（主界面图标 / 页签 / 子按钮） |
| 消除条件 | 领取、查看、操作完成、过期、跨日刷新后何时熄灭 |
| 刷新时机 | 与日切、活动阶段、次数重置相关的重新计算规则 |
| 优先级 | 多红点并存时的展示策略（若项目有统一规范则引用，否则标注待确认） |

与界面原型、流程图独立：有 UI 的模块常见需要红点；无 UI 模块通常不涉及，但须在判定中显式说明。

## 数据兼容（线上项目）

### 文档级：第 2 章总则

阶段 1 产出或确认项目级约定，各模块在 `数据兼容` 小节引用而非重复定义。常见条目：

| 条目 | 说明 |
|------|------|
| 客户端版本策略 | 老客户端连新服的最低支持范围、强制更新点、协议/字段向前兼容原则 |
| 转服策略 | 活动中能否转服、进度/奖励/排行榜是否迁移、未领奖励如何处理 |
| 联盟/社交归属 | 退盟、加盟、解散对联盟维度数据的影响与清理时机 |
| 热更与活动窗口 | 热更前后活动状态、配表版本、进行中局/任务是否中断或回滚 |
| 跨服与合服 | 跨服玩法数据挂载点、合服后 ID/排行/邮件补偿原则（若适用） |

项目尚无定论的条目写入「待确认」，不擅自编造具体策略。

### 模块级：逐系统核对

确认规则时，对照下面场景判断本模块是否受影响（按需取用，无关场景不写）：

| 场景 | 须明确的问题 |
|------|----------------|
| 老客户端 × 新服务器 | 缺字段/旧协议时默认行为、是否降级展示、是否拒绝参与 |
| 活动中转服 | 进度、次数、未领奖励、排行、联盟贡献是否保留或折算 |
| 退盟 / 加盟 / 解散 | 联盟绑定数据清零、冻结还是随玩家迁移 |
| 热更 / 配表变更 | 进行中状态如何结算、旧档数据如何迁移或作废 |
| 断线重连 / 重复请求 | 幂等与重复领取防护（与本模块相关的部分） |

写法：引用第 2 章总则 + 本模块特例；无特例且完全遵循总则时可写「遵循第 2 章数据兼容总则，无额外约定」。

## 原型图标注硬性规则

当任务涉及读取、标注、解释截图/原型图，并生成规则说明时，必须遵守：

1. **先看原图，再写规则**：逐张复核截图中实际可见的 UI、文案、数值、图标和布局，不凭相似系统或开发文档经验补全图中不存在的内容。
2. **区分图中内容与规则说明**：文档表格固定使用 `标号 / 图中内容 / 规则说明`。`图中内容` 只写截图可见事实；从开发文档推导出的逻辑只能写在 `规则说明`。
3. **标号一一对应**：每条 `图中内容` 必须有标注图内对应编号；标注图右侧说明和 Markdown 表格必须来自同一套内容，避免图文漂移。
4. **禁止编造图中元素**：如果图里没有 VIP、设置、成就、加成等入口，不得写入 `图中内容`；如需提出推测或产品建议，放入“待确认/注意”或规则说明。
5. **生成后必须验证**：至少检查标注图数量、表格是否都有标号列、已知错误词是否仍存在，并抽查用户指出过的问题图。
