---
name: mindtodoc
description: 把粗糙的移动游戏想法（脑图、框架文档、参考游戏名）转化为功能策划案及派生文档。读取 input/ 素材，澄清规则后输出主案 + 配置表结构/界面标注等派生 md，可选同步钉钉。Use when 用户要把游戏创意整理成开发文档、生成游戏开发文档、game design doc、功能策划案。
---

# MindToDoc

把 `input/`（或既有 `*-开发文档.md`）下的粗糙游戏想法，转化为可评审策划案。

## 本技能定位

- **职责**：从素材到成稿的工作流 —— `input/` 扫描、逐模块澄清、产出路径、迁移旧稿。
- **格式来源**：采用 system-design-doc 文档格式（章节结构、内容边界、界面标注、钉钉落地）。**相关规范与脚本已随附进本技能 `references/` 与 `scripts/`，self-contained，不依赖任何外部路径或技能。**
- **默认产出**：主案 + 配置表结构 / 界面标注等派生 md，可选同步钉钉。

---

## SSOT 地图

只在一个地方维护细则，避免同一规则多处漂移。SKILL.md 只负责跨 reference 的工作流与判断分流，规则细节一律下沉到对应 reference：

- `SKILL.md`：工作流、写作判断、跨 reference 的硬性总则、何时读取哪个 reference。
- `references/feature-spec-boundaries.md`：主功能案内容边界——写什么、不写什么、数值奖励粒度。
- `references/feature-spec-writing.md`：起草顺序、功能/界面规则分工、常见错误。
- `references/mobile-system-rules-reference.md`：逐系统交付物判定、红点规则、线上数据兼容、配置表边界。
- `references/k1-common-configs.md`：K1 项目公共配置表清单（活动框架 / 通行证 / 排行 / 礼包 / 邮件 / 联盟 / KVK 等），写派生 `-配置表结构.md` 的「公共 / 外部依赖」时直接挑用。
- `references/ui-annotation-reference.md`：UI 识图、归类、标注、上传、左图右文、界面下方 key 录入表。
- `references/dingtalk-sync-reference.md`：钉钉 MCP、`md2jsonml.py`、payload 限制、jsonml/markdown 双路径、补 ind、补表、补图。
- `scripts/md2jsonml.py`：转换行为的可执行真相源。

> **维护说明**：`references/` 与 `scripts/md2jsonml.py` 自 system-design-doc 技能 vendor 而来；上游更新时手动同步，保持本技能自包含。
> **多语言 key**：本技能**不绑定**任何项目的 localization 文件。新建 key 时按所在项目的多语言规范命名与查重——K1 用**功能名前缀**（如 `ActvSoccer_`），X1/X15 用 `LC_<页签>_`；项目未提供查重源时在 key 表备注「未查重」，不假装已查。

---

## 产出位置

| 产物 | 路径 | 说明 |
|------|------|------|
| **主功能案** | `output/<功能名>.md` | 变更记录 + 简介 + 关键决策 + 详细规则 + 按需项 |
| **配置表结构**（派生） | `output/<功能名>-配置表结构.md` | 仅数值策划 Excel；含文档边界与概览 |
| **界面标注**（派生） | `output/<功能名>-界面标注.md` | 左图右文 + 红圈 + key SSOT |
| **界面素材** | `output/ui-annotation/assets/` | `{界面ID}.png`、`*_annotated.png/json` |
| **其他派生**（按需） | `output/<功能名>-checklist.md` 等 | Checklist、美术、程序结构 |
| **旧版（只读素材）** | `output/<游戏名>-开发文档.md` | 迁移源，新稿不覆盖 |

模板：`templates/doc-structure.md`（主案）、`templates/feature-module.md`（模块章）、`templates/derived-config-tables.md`、`templates/derived-ui-annotation.md`。

---

## 文档粒度（硬性）

- **一个完整功能 = 一份主功能案 + 多个派生文档**，不按子模块拆成多份独立策划案。
- 仅下列内容拆 **派生文档**：配置表字段、界面标注、Checklist、美术需求、音效需求、BI日志需求。

---

## 边界归属（主案 vs 派生）

**「边界」不都在派生文档。** 写错位置会导致主案缺规则或派生越权：

| 边界类型 | 写什么 | 写在哪 |
|----------|--------|--------|
| **边界情况处理** | 异常态、重复请求、活动结束、断线、满/空/禁用态等 | **主案** — `# 边界情况处理` 或各模块 |
| **数据兼容** | 转服、热更、老客户端、幂等（线上） | **主案** — 总则 + 模块特例 |
| **功能 / 系统边界** | 跨系统划分、依赖（合同 vs 球队、复用 X1 竞猜） | **主案** — `# 关键决策` + 模块规则 |
| **数值与奖励边界** | 触发、领取条件；不写最终数值 | **主案** — 「由配置表定义」 |
| **配置表文档边界** | Excel 含什么、不含存档/运行时/协议 | **派生** `-配置表结构.md` · `## 文档边界` |
| **界面规则边界** | 控件、布局、key、红点表现 | **派生** `-界面标注.md` |

**速记**：玩法异常怎么判 → **主案**；表策划填什么 → **配置表派生**；按钮文案布局 → **界面标注派生**。

---

## 文档章节模型（采自 system-design-doc）

| 章节 | MindToDoc 落地 |
|-------------------|----------------|
| A 变更记录 / B 简介 / C 详细规则 | 主案必写；阶段 1–2 |
| 关键决策表 | 主案 `# 关键决策`（含文档粒度、配表边界等） |
| D 手游专项 | 流程图、红点、数据兼容、配置表**名称与用途**（字段进派生） |
| E 界面设计 | 主案占位 + 子界面清单；控件 → 界面标注派生 |
| F BI / G 待确认 | 按需；真待定才写 |
| 配置表 | 主案表名/用途；Excel 字段 + 文档边界 → 配置表派生 |
| 界面标注 | **不用 HTML 原型**；供图 → 标准流程 |
| 钉钉 | 本地 md SSOT；按需 `references/dingtalk-sync-reference.md` |

起草前必读：`references/feature-spec-boundaries.md`、`references/feature-spec-writing.md`；手游/红点/兼容/配表 → `references/mobile-system-rules-reference.md`。

---

## 流程

### 阶段 0 · 读取输入

扫描 `input/` 及用户指定的既有 md/xlsx。若全无素材：提示放入 `input/` 或指定旧稿，停止不硬跑。
扫描 `output/` 是否已有 `*-开发文档.md`。若有则应该作为本次修订的基础。


### 阶段 1 · 确认整体框架

1. **澄清意图**：确认系统名、所属项目、一句话定位、目标玩家行为。信息不足先问，不臆造。
2. **问参考玩法**：主动问“有没有参考游戏的某个玩法作为参考？”有则先梳理骨架，无则走标准设计。
3. **批判性分析**：对参考规则或用户给出的规则主动指出不合理处、优化空间和遗漏边界，给建议而非照抄。
4. **关键决策**：把开放条件、核心循环、与其他系统的边界做成**决策表**（维度｜决策｜关键约束/备注）。有分歧时写明为什么选它。

产出：**变更记录、简介、关键决策**；线上项目写 **数据兼容总则**。

### 阶段 2 · 逐模块深入

 **章节顺序**
*A. 变更记录*
表格列固定：版本｜变更类型｜变更内容｜经办人｜时间。变更内容写实，不写"完善规则"这类空话。

*B. 简介*
设计目的：从玩法价值讲清这个系统为什么存在。
功能简述：用分层 bullet 勾出系统由哪几块构成、整体怎么转。

*C. 详细规则*
按模块清单进行循环：

**确认规则**（必选）—— 触发条件、分支、不满足提示；红点/数据兼容必考虑或显式跳过。
- 按真实对象与阶段拆分规则。
- 每条规则尽量具体到**触发条件 + 各分支 + 不满足提示**。
- 单列「边界情况处理」章节，覆盖状态变化、关系变化、服务器维护、异常态、重复请求等。
- 需要时补文案包装：对外命名、世界观包装、提示语语气。
- 需要时补流程图、线上数据兼容处理。
- 只收用户明确未定的真待定项。内部讨论、草稿记录和未决问题不写进主功能案正文。无真待定项时本节不写。

**判定交付物** —— 流程图、红点、兼容。

**追加主案** —— 配置表字段写入派生；跳过项不写空节。

用户确认 → 下一模块。

### 阶段 3 · 派生文档与收尾

**配置表结构**：`templates/derived-config-tables.md` — 概览、各模块字段。
**界面 E 节**：主案占位 + 子界面清单；**二选一** — 供图走阶段 4，或明确待补图。
**其他派生**：Checklist、美术等 + `# 派生文档说明`。
主案补 **边界情况处理**。

### 阶段 4 · 界面标注（用户提供图片后）

```
供图 → assets/{界面ID}.png → 识图+归类+编号 → *_annotated.png/json
→ 用户审核 → 写入 -界面标注.md（左图右文 + key 表）→（可选）钉钉
```

细则：`references/ui-annotation-reference.md`、`templates/derived-ui-annotation.md`。无图不写控件；新建 key 按所在项目的多语言规范命名与查重。

**截图标注硬性规则**（从旧版平铺表升级时仍适用）：

1. **先看原图再写规则**；`图中内容` 只写截图可见事实，逻辑推导只进说明列。
2. **标号一一对应**；归类合并后右栏用二级 bullet，禁止平铺每个像素级控件。
3. **禁止编造图中元素**；无图则主案/E 节不写控件级内容。
4. 标注完成后须用户对照原图审核，再写入派生 md / 同步钉钉。

### 阶段 5 · 钉钉落地（按需）

先改本地 md，再 `references/dingtalk-sync-reference.md` + `scripts/md2jsonml.py`。

---

## 外部技能与降级

| 能力 | 优先 | 降级 |
|------|------|------|
| 流程图 | mermaid 技能；可用 `mmdc` 校验语法 | 手写 Mermaid + 注明「未校验」 |
| 界面 | 用户截图 + 多模态识图 + PIL 红圈标注 → `-界面标注.md` | 主案 E 节仅占位子界面清单 |
| 钉钉 | 钉钉 MCP + md2jsonml | 仅本地 md |

---

## 主案 / 派生写作要点

**整稿覆盖/大幅重写**

- ≥ 200 行的全文覆盖**用 Bash heredoc 多段追加**(`cat >> file.md << 'EOF' ... EOF`),不要一次 Write 大字符串。一次 Write 20k+ 字符极易触发 API Error 然后被迫拆段重写,反而更慢。第一段用 Write 起头,后续段全部 heredoc 追加;最后跑一次校验脚本(如 `scripts/dingtalk_md_unescape.py --check`)。
- 钉钉 ↔ 本地双向同步必须做转义清洗,详见 `references/dingtalk-sync-reference.md §七`。

**主案**

- 强调：`**加粗**`、`{{red:红字}}`；成段叙述 + 分层 bullet，不堆碎句。
- 主功能案的功能名只放在 md 文件名和钉钉文档名中，正文不额外写同名总标题；正文真正章节从 H1 开始，例如 `# 变更记录`、`# 简介`、`# 详细规则`。
- 标题用干净的 H1/H2/H3/H4 层级，不手写"一、""1.1"。钉钉落地时由 `list.autoLevel=true` 生成自动编号，H1 从 `1.` 开始。
- 规则项优先用两级无序列表：第一级为规则项名，第二级为详细说明。
- 表格用于变更记录、属性分类、配置投放、边界情况、决策记录、BI 字段、多语言 key。
- 源 md 只使用两种行内强调：`**加粗**` 与 `{{red:红字}}`。转换和钉钉落地细节见 `references/dingtalk-sync-reference.md`。
- 凡设计必附"为什么"。必要时显式列"必须避免的方向"，并在源 md 中用 `{{red:...}}` 标记。
- 写法要像人写的策划案：成段叙述与分层 bullet 结合，讲清因果与条件，不堆标签碎句。
- 默认假设是"一切都该写清楚"。「待确认」是例外，不是兜底。
- 只有用户明确未定，才标待定；待定项必须写明待谁拍板。
- 不用"待确认"掩盖没问够或没写细；能继续写细就继续写，必须决策才提问。
- 待定项一旦确认，结论必须合并回主功能案，不能只停留在临时记录里。
- 最终生成阶段不应保留未处理的待定项；确实不处理的写入"本期不处理"或后续版本范围。

自检：如果你正要写「待确认」，先问自己这是用户明确没定，还是你没问、没写细。

**配置表派生**

- 含：Excel 页签/字段、概览、公共依赖；不含存档/运行时/协议。
- 「公共 / 外部依赖」表直接从 `references/k1-common-configs.md` 挑表填入；命中即用，不复制内容到派生文档。

**界面标注派生**

- 含：标准流程、子界面进度表、单屏左图右文模板；旧稿平铺表标「遗留，待升级」。

---

## 决策变更追溯

长讨论中常会推翻主案已落地的决策（例如本次"整关锁定模式"被改为"默认模式 + 随时可改"）。同一规则在主案的关键决策表、详细规则、流程图、配置表派生中往往出现 3–5 处，临时改一处易漏。

**约定**：对话中一旦确认推翻已写入主案的某条决策，**先写"决策变更日志"再动主案**：

- 位置：长讨论的过程档（如 `output/<功能名>-<专题>-讨论稿.md`）末尾的「主案待修订点」表。
- 格式：列「文件 ｜ 章节 ｜ 旧表述 ｜ 新表述要点」，每个受影响章节一行。
- 用法：作为修订主案的 checklist；改完一行勾一行。
- 主案落地后，把该日志合并进变更记录表的对应版本备注，过程档保留为档。

**判定阈值**：仅在"决策已写入主案"且"修订涉及 ≥ 2 个章节"时使用；单点小改不需要这套流程。

**讨论稿过程档**：超过一节（约 4–6 个共识维度）的长讨论建议 `output/<功能名>-<专题>-讨论稿.md` 边谈边沉淀；每完成一节就 append 共识小结表，避免上下文压缩后丢失。最终成稿合并入主案后，过程档不删除、保留作档。

---

## 自检清单

**整体**

- [ ] 工作流走完：澄清意图、问参考玩法、批判性分析、关键决策、逐章节判定交付物、起草自检、按需落地。
- [ ] 没有用"待确认"掩盖没问够或没写细；真待定项标明待谁拍板；未定数值写"配置"，没编数字。
- [ ] 涉及对应领域时，已读取并按 reference 落地：手游按需项 → mobile reference；界面 → 先索图二选一 + ui-annotation reference；多语言 → 按所在项目多语言规范命名查重；钉钉 → dingtalk reference。
- [ ] 主功能案面向外部读者，没塞内部讨论、技术方案、AI 视图等派生内容。
- [ ] 写法成段、有因果、可评审，无碎句堆砌和口水话。

**主案**

- [ ] ABC + 关键决策齐全；边界情况 / 数据兼容
- [ ] 无完整字段 schema、无平铺 UI 大表、规则层无 key 名
- [ ] 待确认仅真待定项

**派生**

- [ ] `-配置表结构.md` 含文档边界与概览；无运行时/存档/协议
- [ ] `-界面标注.md` 含流程与进度表；有图界面已审核后左图右文
- [ ] `# 派生文档说明` 链接齐全

**流程**

- [ ] 阶段 1 问过参考玩法
- [ ] 已对照本节自检清单走完
