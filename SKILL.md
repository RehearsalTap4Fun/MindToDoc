---
name: mindtodoc
description: 把粗糙的移动游戏想法（脑图、框架文档、参考游戏名）转化为 system-design-doc 格式的功能策划案及派生文档。读取 input/ 素材，澄清规则后输出主案 + 配置表结构/界面标注等派生 md，可选同步钉钉。Use when 用户要把游戏创意整理成开发文档、生成游戏开发文档、game design doc、功能策划案、从旧版 MindToDoc 迁移。
---

# MindToDoc

把 `input/`（或既有 `*-开发文档.md`）下的粗糙游戏想法，转化为 **system-design-doc 格式** 的可评审策划案。

## 本技能定位

- **职责**：从素材到成稿的工作流 —— `input/` 扫描、逐模块澄清、产出路径、迁移旧稿。
- **格式来源**：采用 system-design-doc 文档格式（章节结构、内容边界、界面标注、钉钉落地）。**相关规范与脚本已随附进本技能 `references/` 与 `scripts/`，self-contained，不依赖任何外部路径或技能。**
- **默认产出**：主案 + 配置表结构 / 界面标注等派生 md，可选同步钉钉。

## 随附文件（self-contained）

| 文件 | 用途 | 何时读 |
|------|------|--------|
| `references/feature-spec-boundaries.md` | 主案内容边界：写什么、不写什么、数值奖励粒度 | 起草前必读 |
| `references/feature-spec-writing.md` | 起草顺序、功能/界面规则分工、常见错误 | 起草前必读 |
| `references/mobile-system-rules-reference.md` | 逐系统交付物判定、红点、线上数据兼容、配置表边界 | 涉及手游入口/红点/线上/配表 |
| `references/ui-annotation-reference.md` | UI 识图、归类、红圈标注、上传、左图右文、key 录入表 | 阶段 4 界面标注 |
| `references/dingtalk-sync-reference.md` | 钉钉 MCP、`md2jsonml.py`、payload 限制、补 ind/表/图 | 阶段 5 钉钉落地 |
| `scripts/md2jsonml.py` | md → 钉钉 jsonml 转换器（可执行真相源） | 钉钉落地 |
| `templates/*.md` | 主案 / 模块 / 配置表 / 界面标注模板 | 起草各阶段 |

> **多语言 key**：本技能**不绑定**任何项目的 localization 文件。新建 key 时按所在项目的多语言规范命名与查重；项目未提供查重源时在 key 表备注「未查重」，不假装已查。
>
> **维护说明**：`references/` 与 `scripts/md2jsonml.py` 自 system-design-doc 技能 vendor 而来；上游更新时手动同步，保持本技能自包含。

---

## 旧版 MindToDoc vs system-design-doc（差异总表）

| 维度 | 旧版 MindToDoc | system-design-doc 定稿 | MindToDoc 现做法 |
|------|----------------|------------------------|------------------|
| **文档对象** | 整游戏一份「开发文档」 | 以**功能/系统**为单位的策划案 | 默认**整功能一份主案**（活动、玩法包不拆成 N 份独立策划案） |
| **正文结构** | 固定四段：概述 / 核心机制 / 系统设计 / Checklist | ABC 必写 + DEFG 按需；**顺逻辑组织，不套僵化模板** | 对齐 ABC + 关键决策 + 按需 DEFG |
| **文档标题** | 正文 `# {{游戏名}} 开发文档` | 功能名只在**文件名**；正文从 `# 变更记录` 起 | 删除 H0 总标题 |
| **元信息块** | `> 平台 \| 对标 \| 日期` | 无固定块 | 拆入简介 / 关键决策 / 变更记录 |
| **模块写法** | `### 3.x` + 固定四小节 | 详细规则内 H2/H3 按对象分章 | `templates/feature-module.md`；**不**固定四件套 |
| **模块四小节** | 规则 → 流程图 → **HTML 原型** → **内嵌字段表** | 按需判定；界面走截图标注；字段不进主案 | 流程图按需；界面→派生；字段→配置表派生 |
| **界面** | `output/prototypes/*.html` + OSS 截图平铺表 | 红圈标注 + **左图右文** + key 表；无图不写控件 | `-界面标注.md` + `ui-annotation/assets/` |
| **配置/数据** | 主案内完整字段 schema | 主案只写表名/用途；字段 schema 派生 | `-配置表结构.md`（**仅数值策划 Excel**） |
| **Checklist / 美术** | 主文档第 4、5 章 | 不进主功能案 | 独立派生 md，`# 派生文档说明` 挂链 |
| **程序结构** | 混在数据表节 | 研发派生文档 | 不进配置表派生 |
| **红点 / 兼容** | 模块内必考虑项 | D 按需 + mobile reference | 保留必考虑；写法跟 reference |
| **待确认** | 较宽松 | 真待定才写；不用「待确认」兜底没问够 | 对齐定稿 §三 |
| **钉钉** | 无 | 本地 md SSOT → MCP / md2jsonml | 阶段 5 按需 |
| **写作顺序** | 可从模块四小节齐头并进 | 先功能规则 → 再界面；先目标边界 → 再配置 | 阶段 2 写规则，阶段 3–4 写派生 |

---

## 定稿共识（对比与迁移过程中拍板）

以下优先级高于旧版 MindToDoc 习惯或早期迁移草稿：

1. **文档粒度**：一个完整功能（如「2026 世界杯主题活动」）= **一份主功能案**；创角、关卡、养成等是「详细规则」内章节，**不是**每个模块各一份独立 `.md`。
2. **配置表派生边界**：`-配置表结构.md` **只**含数值/关卡/活动策划在 Excel 填写的表；**不含** `player_profile`、运行时态、协议、DB、程序只读枚举定义。
3. **配置表概览**：派生文档须含文件清单、公共/外部依赖、专用表一览与关联（见 `templates/derived-config-tables.md`）。
4. **界面派生**：主案 E 节仅占位 + 子界面清单；控件级内容 SSOT 在 `-界面标注.md`；用户提供图后走标准流程（识图→归类→红圈→审核→左图右文→可选钉钉）。
5. **边界归属**：玩法边界情况、数据兼容、功能边界 → **主案**；配表文档边界 → **配置表派生**；控件/key → **界面标注派生**（见 §边界归属）。
6. **功能 vs 界面规则**：解锁、结算、状态、数值判定 → 主案；按钮态、布局、文案 key、红点表现 → 界面派生。
7. **旧稿迁移**：`*-开发文档.md` 保留作素材；拆为 `output/<功能名>.md` + 派生文档，不直接覆盖旧文件。

**迁移样例**（世界杯主题活动）：`output/system-design-doc-samples/`

---

## 旧模板 → 定稿迁移对照（段落级）

从 `*-开发文档.md` 或旧 `templates/` 迁移时按此映射：

| 旧 MindToDoc 位置 | 定稿落点 | 怎么改 |
|-------------------|----------|--------|
| `# {{游戏名}} 开发文档` | （删除） | 功能名仅保留在文件名 |
| `> 平台 \| 对标 \| 日期` | 简介 + 变更记录 + 关键决策 | 拆字段，不写固定元信息块 |
| `## 1. 概述` | `# 简介` | 收窄为**本功能**设计目的与功能简述 |
| `## 2. 核心玩法机制` | `# 详细规则` → `## 全局玩法机制` | 整功能级循环/全局规则放此；模块特例进各模块章 |
| `## 3. 系统设计` | `# 详细规则` 下各 `## 模块名` | 去掉 `3.x` 编号感；不拆独立文件 |
| `#### 规则与逻辑` | 模块 `##` 正文 | 补触发条件、分支、不满足提示；拆出界面表述 |
| `#### 流程图` | 模块 `### 流程图` | 按需保留；不需要则删节 |
| `#### 界面原型` | **不进主案** | → `-界面标注.md`；HTML 链接删除；平铺表升级左图右文 |
| `#### 数据结构 / 配置表` | **不进主案** | Excel 字段 → `-配置表结构.md`；运行时/存档 → 程序派生 |
| `## 4. Checklist` | 派生 `-checklist.md` | 主案 `# 派生文档说明` 挂链 |
| `## 5. 美术需求` | 派生美术 md | 不进主案 |
| `## 6. 修订记录` | `# 变更记录` 表格 | 合并为版本行 |
| 模块内 `[3.x 模块名]` 交叉引用 | 同文档 `## 模块名` 或简述 | 去掉章节编号式引用 |
| 规则层具体 key 名 | 界面标注派生 key 表 | 主案规则不挂 key |
| 内嵌 TODO 数值 | 「由配置表定义」+ 派生表 | 主案不编最终数字 |

---

## 产出位置

| 产物 | 路径 | 说明 |
|------|------|------|
| **主功能案** | `output/<功能名>.md` | 变更记录 + 简介 + 关键决策 + 详细规则 + 按需项 |
| **配置表结构**（派生） | `output/<功能名>-配置表结构.md` | 仅数值策划 Excel；含文档边界与概览 |
| **界面标注**（派生） | `output/<功能名>-界面标注.md` | 左图右文 + 红圈 + key SSOT |
| **界面素材** | `output/ui-annotation/assets/` | `{界面ID}.png`、`*_annotated.png/json` |
| **其他派生**（按需） | `output/<功能名>-checklist.md` 等 | Checklist、美术、程序结构 |
| **旧版（只读素材）** | `output/<游戏名>-开发文档.md` | 迁移源，新稿不覆盖 |

模板：`templates/doc-structure.md`（主案）、`templates/feature-module.md`（模块章）、`templates/derived-config-tables.md`、`templates/derived-ui-annotation.md`。

---

## 文档粒度（硬性）

- **一个完整功能 = 一份主功能案**，不按子模块拆成多份独立策划案。
- 内部模块是主案 **「详细规则」内的 H2/H3**，不是 `3.1 / 3.2` 各一份文件，也**不是** system-design-doc 早期示例里「每系统一 md」的拆法。
- 仅下列内容拆 **派生文档**：配置表字段、界面标注、Checklist、美术、程序/协议结构。

---

## 边界归属（主案 vs 派生）

**「边界」不都在派生文档。** 写错位置会导致主案缺规则或派生越权：

| 边界类型 | 写什么 | 写在哪 |
|----------|--------|--------|
| **边界情况处理** | 异常态、重复请求、活动结束、断线、满/空/禁用态等 | **主案** — `# 边界情况处理` 或各模块 |
| **数据兼容** | 转服、热更、老客户端、幂等（线上） | **主案** — 总则 + 模块特例 |
| **功能 / 系统边界** | 跨系统划分、依赖（合同 vs 球队、复用 X1 竞猜） | **主案** — `# 关键决策` + 模块规则 |
| **数值与奖励边界** | 触发、领取条件；不写最终数值 | **主案** — 「由配置表定义」 |
| **配置表文档边界** | Excel 含什么、不含存档/运行时/协议 | **派生** `-配置表结构.md` · `## 文档边界` |
| **界面规则边界** | 控件、布局、key、红点表现 | **派生** `-界面标注.md` |
| **程序 / 协议结构** | 存档、运行时、接口 | **研发派生**；主案只挂链 |

**速记**：玩法异常怎么判 → **主案**；表策划填什么 → **配置表派生**；按钮文案布局 → **界面标注派生**。

---

## 文档章节模型（采自 system-design-doc）

| 章节 | MindToDoc 落地 |
|-------------------|----------------|
| A 变更记录 / B 简介 / C 详细规则 | 主案必写；阶段 1–2 |
| 关键决策表 | 主案 `# 关键决策`（含文档粒度、配表边界等） |
| D 手游专项 | 流程图、红点、数据兼容、配置表**名称与用途**（字段进派生） |
| E 界面设计 | 主案占位 + 子界面清单；控件 → 界面标注派生 |
| F BI / G 待确认 | 按需；真待定才写 |
| 配置表 | 主案表名/用途；Excel 字段 + 文档边界 → 配置表派生 |
| 界面标注 | **不用 HTML 原型**；供图 → 标准流程 |
| 钉钉 | 本地 md SSOT；按需 `references/dingtalk-sync-reference.md` |

起草前必读：`references/feature-spec-boundaries.md`、`references/feature-spec-writing.md`；手游/红点/兼容/配表 → `references/mobile-system-rules-reference.md`。

---

## 流程

### 阶段 0 · 读取输入

扫描 `input/` 及用户指定的既有 md/xlsx。若全无素材：提示放入 `input/` 或指定旧稿，停止不硬跑。

**若存在旧版 `*-开发文档.md`**：按 §旧模板→定稿迁移对照 拆为主案 + 派生，保留旧文件。

### 阶段 1 · 确认整体框架

汇报：功能定位、核心玩法、目标平台、**模块清单**（→ 主案章节，非独立文件）。

同步确认：文档粒度（整功能一份）、是否线上项目、参考玩法、待确认假设。

产出：**变更记录、简介、关键决策**；线上项目写 **数据兼容总则**。

### 阶段 2 · 逐模块深入（同一主案内）

对模块清单循环：

1. **确认规则**（必选）—— 触发条件、分支、不满足提示；红点/数据兼容必考虑或显式跳过。
2. **判定交付物** —— 流程图、配置表说明（主案指针）、红点、兼容；**界面只登记子界面 ID**。
3. **追加主案** —— 配置表字段写入派生；跳过项不写空节。
4. 用户确认 → 下一模块。

**功能规则 vs 界面规则**：见 §边界归属。

### 阶段 3 · 派生文档与收尾

1. **配置表结构**：`templates/derived-config-tables.md` — 文档边界、概览、各模块字段。
2. **界面 E 节**：主案占位 + 子界面清单；**二选一** — 供图走阶段 4，或明确待补图。
3. **其他派生**：Checklist、美术等 + `# 派生文档说明`。
4. 主案补 **边界情况处理**（若阶段 2 未单列）。
5. 走 §自检清单。

### 阶段 4 · 界面标注（用户提供图片后）

```
供图 → assets/{界面ID}.png → 识图+归类+编号 → *_annotated.png/json
→ 用户审核 → 写入 -界面标注.md（左图右文 + key 表）→（可选）钉钉
```

细则：`references/ui-annotation-reference.md`、`templates/derived-ui-annotation.md`。无图不写控件；新建 key 按所在项目的多语言规范命名与查重。

**截图标注硬性规则**（从旧版平铺表升级时仍适用）：

1. **先看原图再写规则**；`图中内容` 只写截图可见事实，逻辑推导只进说明列。
2. **标号一一对应**；归类合并后右栏用二级 bullet，禁止平铺每个像素级控件。
3. **禁止编造图中元素**；无图则主案/E 节不写控件级内容。
4. 标注完成后须用户对照原图审核，再写入派生 md / 同步钉钉。

### 阶段 5 · 钉钉落地（按需）

先改本地 md，再 `references/dingtalk-sync-reference.md` + `scripts/md2jsonml.py`。

---

## 外部技能与降级

| 能力 | 优先 | 降级 |
|------|------|------|
| 流程图 | mermaid 技能；可用 `mmdc` 校验语法 | 手写 Mermaid + 注明「未校验」 |
| 界面 | 用户截图 + 多模态识图 + PIL 红圈标注 → `-界面标注.md` | 主案 E 节仅占位子界面清单 |
| 钉钉 | 钉钉 MCP + md2jsonml | 仅本地 md |

> **已废弃（勿再写进流程）**：旧版「`ui-ux-pro-max` → `output/prototypes/*.html`」界面原型路径。界面一律走 **E 节 + 界面标注派生**（`references/ui-annotation-reference.md`）。

---

## 主案 / 派生写作要点

**主案**

- 正文从 `# 变更记录` 起；规则层不挂 key 名。
- 数值未定 → 「由配置表定义」；边界情况、数据兼容、功能边界留主案。
- 强调：`**加粗**`、`{{red:红字}}`；成段叙述 + 分层 bullet，不堆碎句。

**配置表派生**

- 开头 `## 文档边界` 只描述**配表范围**，不替代主案边界情况处理。
- 含：Excel 页签/字段、概览、公共依赖；不含存档/运行时/协议。

**界面标注派生**

- 含：标准流程、子界面进度表、单屏左图右文模板；旧稿平铺表标「遗留，待升级」。

---

## 自检清单

**格式对齐**

- [ ] 已读 `references/feature-spec-boundaries.md`、`feature-spec-writing.md` 及本技能 §定稿共识
- [ ] 整功能一份主案；未误拆为多份独立策划案
- [ ] 旧四段式 / HTML 原型 / 主案内嵌字段表已迁出

**主案**

- [ ] ABC + 关键决策齐全；边界情况 / 数据兼容 / 功能边界在主案
- [ ] 无完整字段 schema、无平铺 UI 大表、规则层无 key 名
- [ ] 待确认仅真待定项

**派生**

- [ ] `-配置表结构.md` 含文档边界与概览；无运行时/存档/协议
- [ ] `-界面标注.md` 含流程与进度表；有图界面已审核后左图右文
- [ ] `# 派生文档说明` 链接齐全

**流程**

- [ ] 阶段 1 问过参考玩法；界面 E 节已二选一
- [ ] 已对照本节自检清单走完
