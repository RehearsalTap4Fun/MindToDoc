## {{模块名}}

{{模块定位一句话}}

<!-- 本模块是所属规则的完整落点；全局阶段和通用条件引用具体总则，只在这里补模块特有分支与反馈。完成全文后执行自动成稿门禁，不把本模块规则再抄到边界或概览表。 -->

- **{{规则项 1}}**：{{触发条件、分支、不满足提示}}
- **{{规则项 2}}**：{{…}}

### 流程图

<!-- 判定需要时保留；否则删除整节 -->

```mermaid
{{flowchart TD}}
```

<!-- 模块内写清业务参数的含义与用途；配置结构是固定派生，核对本模块新增、修改与复用的结构，仅在文件实际存在且已承接时，在主案末尾统一引用。 -->

### 红点规则

<!-- 判定需要时保留：触发/消除/展示位置/刷新；否则删除整节 -->

### 数据兼容

<!-- 判定需要时保留：引用总则 + 本模块特例；否则删除整节 -->
