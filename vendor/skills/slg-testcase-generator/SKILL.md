---
name: slg-testcase-generator
description: |
  SLG手游测试用例自动生成。支持四种功能类型：A(新系统) B(功能优化) C(礼包) D(运营活动)。
  触发场景：(1)用户提供PRD/需求文档要求生成测试用例 (2)分析配置表生成验证点
  (3)对比人工用例优化AI生成质量 (4)用户说"生成用例"/"测试用例"/"PRD分析"
  (5)静态验收/static acceptance、本地 Markdown 用例或测试用例证据
---

# SLG 测试用例生成

---

## Mode selection

- 当请求来自 `review-k1-function`，或明确用于快速静态预检时，进入快速静态模式：硬预算 **2 分钟**，读取
  `references/static_acceptance.md` 后生成或复用**最多 20 条**精简用例，并在输出
  Markdown 后**立即返回**。
- 快速静态模式中，开发文档是需求唯一权威。已有用例只复用、不重新生成；没有
  用例时才生成主流程、关键边界和明显异常场景。任何用例都不能替代、修改或覆盖
  开发文档。
- 快速静态模式不得继续读取下方项目配置、知识库、七步标准流程或文件写入流程，
  也不得创建或更新在线文档/表格。
- 其他请求进入完整用例模式。覆盖范围与交付格式分别判定：指定本地 Markdown／Excel 不代表接受快速模式的限时限量。
- 完整模式沿用下方需求分析、适用场景与覆盖校验；本地交付读 `references/local-delivery.md`，在线 Google Sheets 交付读 `references/file-operations-compact.md`。用户未指定格式且项目未约定时默认本地 Markdown，不自动发布。
- 本节的范围与完成标准优先于下方及引用文件的通用模板：关键词只触发适用性核对；模块和场景须有主案依据，数量区间和“最低条数”均作为估算与遗漏提示，不是验收下限。已确认范围内的缺口当轮修正，不重复询问是否补充。

---

## 🔧 项目配置（使用前必读）

> 本 Skill 为**通用基础层**，适用于所有 SLG 项目。项目特有的配置（路径、ID、特色玩法知识等）存放在各项目工作区的 `testcase-config/` 目录中。

**AI 强制规则**：在执行任何用例生成流程之前，必须先完成以下步骤：

1. 读取当前项目工作区的 `testcase-config/project_config.md`，获取项目级配置（Drive 路径、配置表 ID 等）
2. 检查 `testcase-config/game_knowledge/` 目录，了解项目特色玩法知识库
3. 配置不存在时，本地完整交付仍可按已确认主案继续；确实影响预期结果的项目知识单独澄清。只有用户选择在线交付时才需要 Drive 目录与模板配置，不能因本地生成缺在线配置而停止。

> 本文件中涉及路径/ID 的地方，标注了 `{项目配置.xxx}` 的，均需从项目配置文件中读取实际值。

---

## ⛔ 五条铁律（违反=失败）

| # | 铁律 | 说明 |
|:-:|------|------|
| 1 | **7步流程不可跳过** | 阅读需求→判断复杂度→需求确认→知识库→编写规则→生成用例→按选定媒介交付；本地交付不进入在线写入步骤 |
| 2 | **生成前必须输出检查清单** | 先输出「关键词识别→模板触发→预计用例数」，再生成用例。没有输出分析就直接生成=流程违规 |
| 3 | **有据可查，图片优先** | 界面元素/交互形式必须来自 UX 图，不能凭空假设。**PRD 有配图时，以图片展示的实际形态为准**，禁止仅凭文字描述脑补交互方式。**但图片中的示例数据（如具体任务名称、数值）可能是假图占位**，不等于确定需求——检查项照写，预期结果用通用描述，备注标注待确认 |
| 4 | **检查点该合并须合并，密度不要过高** | 同一验证维度、仅前置条件不同的应合并为一个检查点，不要拆成多个「仅 1 条用例」的检查点。检查点名称必须比模块名更具体。详见 [core_writing_rules.md](references/core_writing_rules.md)「模块与检查点划分规则」+「检查点合并与命名规范」 |
| 5 | **🔴 执行成本最低** | 检查点必须按状态流推进，同一前置状态下能验的全部验完再进下一状态，**禁止按 UI 元素类型拆检查点导致测试人员反复准备前置数据**。详见 [core_writing_rules.md](references/core_writing_rules.md)「执行成本最低原则」 |

---

## 🔴 核心规则（10条）

| # | 核心规则 | 说明 |
|:-:|----------|------|
| 1 | **元素全覆盖** | 界面看到什么验证什么（按钮/图标/文本/数值） |
| 2 | **状态全拆分** | 多状态每种单独验证（高亮/置灰/A>B/A=B/A<B） |
| 3 | **操作链全跟踪** | 一个操作触发后完整跟踪所有响应 |
| 4 | **入口必须3条** | 图标 + 名称 + 点击响应 |
| 5 | **新功能验正反** | 开启时显示 + 不开启时不显示，都要验证 |
| 6 | **配置必标ID，数值用n** | 格式：`配置表XXXX-字段名`，不能只写"符合配置"。**配置表数值禁止写具体值，用 `n` 代替，边界用 `n-1`/`n+1`** |
| 7 | **不确定就标注** | 用 ⚠️待确认 / ⚠️推测 标记 |
| 8 | **置灰必验点击** | 显示置灰 + 点击无响应，两条用例 |
| 9 | **开启前后对比** | 新功能必须验证开启前后行为差异 |
| 10 | **检查点该合并须合并** | 每个检查点通常 2~10 条用例；同一验证维度、仅前置条件不同的须合并，**避免拆成多个仅 1 条的检查点**；检查点名称要比模块名更具体，不得与模块名完全相同 |

> 详细写法规则见 [core_writing_rules.md](references/core_writing_rules.md)

---

## ⚠️ 用例生成强制流程（必须遵循）

> **在生成任何用例之前，必须完成以下检查流程，不可跳过！**

| 步骤 | 内容 | 说明 |
|:----:|------|------|
| **1** | **阅读需求** ⭐ | PRD + UX 图必须完整查看，详见 [1_analysis.md](references/1_analysis.md)。**Google Sheet 读取：必须使用 `scripts/excel_tool.py`**（禁止使用 WebFetch/curl） |
| **2** | **判断功能类型与复杂度** | ①判定功能类型（A新系统/B优化/C礼包/D活动）→ **必须读取**对应策略文件（见下方「功能类型策略」表）。②判断复杂度：小型100-200条 / 中型300-600条 / 大型1000-1500条。**⚠️ 若判定为大型/复杂（>400条或多系统联动），必须走步骤2a** |
| **2a** | **🔴 输出模块图（大型/复杂必做）** | 详见下方「复杂系统专用流程」。**禁止跳过此步直接生成用例！** |
| **2b** | **🔴 参考材料确认（Type B 必做）** | 判定为 Type B（功能优化）时，检查用户是否已提供原版PRD/原版用例。**若未提供，主动向用户确认**。如用户表示"之前生成过"但无链接，使用 Drive 全局搜索历史用例，**搜索结果必须先展示给用户确认后再读取**。详见 [type_b_update.md](references/type_b_update.md)「参考材料获取」+「历史用例自动检索」章节 |
| **3** | **需求确认** ⭐ | 识别待确认内容，详见 [0_requirement_confirm.md](references/0_requirement_confirm.md) |
| **4** | **阅读游戏知识库** | 自动检测关键词触发，详见下方「知识库触发规则」。**涉及战斗/派兵时，必须输出「场景清单」** |
| **5** | **阅读编写规则** ⭐ | **必须**读取 [core_writing_rules.md](references/core_writing_rules.md)，执行关键词识别→触发阅读 [scenario_patterns.md](references/scenario_patterns.md) 中**索引表 + 匹配章节**（⚠️ 不要全文读取，只读索引→跳转触发的章节） |
| **5b** | **生成前检查** ⭐ | **必须**读取 [execution_checklist.md](references/execution_checklist.md)「生成前必做」章节逐条确认。大型系统还需确认「大型复杂系统生成前专项检查」 |
| **6** | **生成用例** | **必须由主 agent 分批生成（禁止子任务生成用例内容）**。小型/中型：一次生成。大型/复杂：按模块图逐模块生成。生成时按步骤2策略文件中的「必选模块清单」逐一覆盖，按需跳转读取 [common_scenarios.md](references/common_scenarios.md) 中对应模板编号。详见下方「用例生成方式选择」 |
| **6b** | **生成后自检** | 按 [execution_checklist.md](references/execution_checklist.md)「生成后自检」+「执行检查清单」逐项检查；**重点自检：是否把本该合并的检查点拆得过密（多个仅 1 条）？** 该合并的须合并后再写入 |
| **7** | **按选定媒介交付** ⭐ | 本地按 [local-delivery.md](references/local-delivery.md)；已授权 Google Sheets 按 [file-operations-compact.md](references/file-operations-compact.md)。有中间 JSON 时可运行 `python scripts/validate_cases.py 文件.json`；先修正覆盖与结构问题再交付 |

### 🔴 复杂系统专用流程（步骤2a，大型/复杂必做）

> **判定标准**：预计用例 >400 条，或 PRD 明确涉及多系统联动（如：英雄+战斗+派兵+战报+副本）

**⚠️ 复杂系统禁止直接生成用例！必须先完成以下步骤：**

#### 第一步：输出《模块图》

必须输出以下格式的模块图，**先锁定覆盖范围**：

```markdown
## 《功能名称》模块图

| # | 模块名称 | 子检查点 | 预计用例数 | 备注 |
|:-:|----------|----------|:----------:|------|
| 1 | 未获得-界面显示 | 入口、主界面元素、详情弹窗 | 25 | |
| 2 | 获得-召唤/解锁 | 召唤流程、首次获得、重复获得 | 15 | |
| 3 | 核心玩法-xxx | xxx | 60 | 含状态矩阵 |
| 4 | 养成-等级/星级 | 升级、满级、配置验证 | 30 | |
| 5 | 派兵界面 | 详情模式、编队、选择限制 | 40 | ⭐必选 |
| 6 | 战斗场景 | 野外pvp/pve、驻防、集结、副本… | 50 | ⭐必选 |
| 7 | 战报显示 | 日志页、详情页、技能显示 | 20 | ⭐必选 |
| 8 | 红点 | 触发、消失、层级联动 | 15 | ⭐必选 |
| 9 | 多入口一致性 | 各界面数据同步 | 10 | ⭐必选 |
| 10 | 数据兼容 | 跨服、老版本、重启 | 20 | |
| 11 | 通用检查 | 异常、兼容、本地化… | 50 | 固定模板 |
| | **合计** | | **335** | |
```

#### 第二步：检查必选模块

模块图输出后，**必须自检以下必选模块是否存在**：

| 必选模块 | 触发条件 | 最少用例数 |
|----------|----------|:----------:|
| **派兵界面** | PRD 涉及英雄/部队/战斗 | 20条 |
| **战斗场景覆盖** | PRD 涉及战斗/技能/buff | 30条 |
| **战报显示** | PRD 涉及战斗 | 10条 |
| **红点** | 有新功能入口 | 10条 |
| **多入口一致性** | 功能在多个界面有入口/展示 | 8条 |
| **配置表验证** | PRD 提及配置表 | 按表覆盖 |
| **数据兼容** | Type A 系统功能 | 15条 |

先核对模块是否属于已确认需求。适用模块缺失时补充；不适用时记录简短依据，不生成虚构场景。上表条数仅为参考，实际以适用规则、状态和异常分支覆盖为准。

#### 第三步：分模块两阶段生成

> **每个模块都走「阶段一：骨架 → 阶段二：填充」，先锁覆盖范围再填细节**

**阶段一：输出用例骨架（仅结构，不写详细步骤和预期）**

按模块图顺序，对当前模块输出骨架：

```markdown
## 模块X：xxx（骨架）

| 编号 | 检查点 | 简要预期 |
|------|--------|----------|
| XX_001 | 入口显示 | 入口图标/名称/点击 |
| XX_002 | 主界面-元素总览 | 包含A、B、C、D元素 |
| XX_003 | 主界面-按钮A | 样式、状态、点击响应 |
| ... | ... | ... |
| 本模块骨架共 X 条 | | |
```

**骨架输出后自检**：
- [ ] 检查点是否与模块图一致
- [ ] 每个界面是否有「元素总览」
- [ ] 必选验证维度是否缺失（红点/配置表/状态切换等）
- [ ] 条数是否与模块图预估接近

**阶段二：按骨架填充完整用例**

将骨架中每条展开为完整的 JSON 格式用例（编号+功能模块+检查点+操作步骤+预期结果+备注）。

**逐模块循环**：模块1（阶段一→阶段二）→ 模块2（阶段一→阶段二）→ … → 所有模块完成 → 汇总合并 → 检查总数

### 🔴 战斗/派兵场景清单（涉及战斗时必做）

> **PRD 中出现「战斗、出征、攻击、驻防、派兵、技能、buff」等关键词时，必须执行**

#### 必须输出「本功能涉及的战斗/派兵场景清单」

```markdown
## 战斗/派兵场景清单

| # | 场景类型 | 是否涉及 | 预计用例数 | 备注 |
|:-:|----------|:--------:|:----------:|------|
| 1 | 野外PVP战斗 | ✅/❌ | | |
| 2 | 野外PVE战斗（怪物/野怪） | ✅/❌ | | |
| 3 | 攻击建筑驻防部队 | ✅/❌ | | |
| 4 | 驻防中立建筑（联盟建筑/采集点/遗迹） | ✅/❌ | | |
| 5 | 城墙防守战斗 | ✅/❌ | | |
| 6 | 托管战斗 | ✅/❌ | | |
| 7 | 集结-发起 | ✅/❌ | | |
| 8 | 集结-加入 | ✅/❌ | | |
| 9 | 战车PVP/PVE | ✅/❌ | | |
| 10 | 竞技场 | ✅/❌ | | |
| 11 | 斗士远征 | ✅/❌ | | |
| 12 | GVG副本 | ✅/❌ | | |
| 13 | 其他副本（霍格博士/围剿boss等） | ✅/❌ | | |
| 14 | 特殊副本（王者之地/AC战场/机甲战场） | ✅/❌ | | |
```

**规则**：
- 勾选「✅」的场景，每个至少 2-3 条用例
- 不涉及的场景标「❌」并说明原因
- **清单中「✅」的场景必须全部在用例中覆盖，否则视为遗漏**

### 🔴 状态/条件矩阵（多状态功能必做）

> **PRD 中涉及「共享、权限、解锁条件、多种限制」等多条件逻辑时，必须执行**

#### 必须先输出状态/条件矩阵

```markdown
## 状态/条件矩阵

### 例：英雄共享限制
| # | 条件 | 预期结果 |
|:-:|------|----------|
| 1 | B在城内空闲 | ✅可共享 |
| 2 | B在城墙驻防 | ✅可共享 |
| 3 | B在副本上阵 | ✅可共享 |
| 4 | B在野外驻扎 | ❌不可共享，弹tips |
| 5 | B在押镖中 | ❌不可共享，弹tips |
| 6 | B在被攻击 | ❌不可共享，弹tips |
| ... | ... | ... |
```

**规则**：矩阵中每一行至少 1 条用例，避免状态组合遗漏。

---

### ⚠️ 生成前检查清单（步骤5-6必须输出）

**在生成用例前，必须先输出以下分析结果：**

```markdown
## 关键词识别与模板触发

### 识别到的关键词
- [关键词1] → 触发 [模板名称]，必须生成 X 条
- [关键词2] → 触发 [模板名称]，必须生成 X 条
- ...

### 核心规则检查
| 规则 | 检查项 | 状态 |
|------|--------|------|
| 元素覆盖 | 界面元素是否逐一验证（入口/按钮/列表/弹窗等） | ✅/❌ |
| 状态拆分 | 多状态场景是否全部展开（高亮/置灰/充足/不足等） | ✅/❌ |
| 操作链完整 | 操作响应是否完整跟踪（动画→UI变化→数据变化→最终状态） | ✅/❌ |

### 预计用例数
- 核心功能：X 条
- 通用兼容：22 条
- 总计：X 条
```

### 🔴 配置数值写法检查（步骤6生成前必做）

> **在填充用例的预期结果和备注之前，必须重新读取 [core_writing_rules.md](references/core_writing_rules.md) 中「配置数值写法规范」章节（搜索关键词 `配置数值写法规范`），并执行以下检查：**

**第一步：整理配置表对照表**

从 PRD 中提取所有涉及配置的数值，整理为对照表：

```markdown
## 配置表对照表

| 数值项 | PRD原文 | 分类 | 写法 |
|--------|---------|------|------|
| 转盘格子数 | 12个格子 | 需求固定（结构） | 直接写"12个格子" |
| 各格子奖励内容 | 紫色宝石×2... | 配置可调（奖励类） | "奖励内容与配置一致" + 备注配置表ID |
| 各次抽取价格 | $0→$1.99→... | 配置可调（价格类） | "价格与配置一致" + 备注配置表ID |
| 概率权重 | 98.08%... | 配置可调（数值类） | "概率与配置一致" + 备注配置表ID |
```

**第二步：按分类决定写法**

| 分类 | 判断标准 | 预期结果写法 | 备注写法 |
|------|----------|-------------|----------|
| **需求固定** | PRD写死且极少变动的结构/逻辑 | 直接写具体值 | 无需标注 |
| **配置可调** | 数值/奖励/价格/概率/时间等 | "与配置一致"，禁止写死具体数值 | `配置表XXXX-字段名` |

**配置可调的常见场景**（必须用"与配置一致"）：
- 数值类：开启天数、触发次数、持续时间、限购次数、CD时间
- 奖励类：奖励道具种类、道具数量、道具品质
- 价格类：礼包价格、商品价格、折扣比例
- 内容类：礼包内容物、活动奖池、掉落物品
- 概率类：掉落率、触发概率、权重

**第三步：检查是否将配置数值误解读为功能特性**

> **🔴 禁止将配置数值解读为功能特性**：当PRD中某个配置数值恰好为特殊值（如$0、100%、1次等）时，**不应将其拆为独立功能模块**，而应统一归入对应流程按配置可调处理。

| 配置数值 | ❌ 错误：拆为独立模块 | ✅ 正确：归入对应流程 |
|----------|----------------------|----------------------|
| 第1次价格=$0 | "首次免费抽取"模块 | 归入"抽取流程"，价格与配置一致 |
| 某道具概率=100% | "必得道具"模块 | 归入"概率验证"，概率与配置一致 |
| 限购次数=1 | "唯一购买"模块 | 归入"限购验证"，次数与配置一致 |

**判断标准**：PRD是否用**文字明确描述**了该机制（如写了"首次免费"）？如果只是数值表中的值，按配置可调处理。

**⚠️ 如果未输出配置表对照表就直接生成用例，视为流程违规！**

**如果未输出以上分析就直接生成用例，视为流程违规！**

### 🔴 用例生成方式选择（步骤6必读）

> **质量优先，禁止为了速度牺牲质量。**

#### 强制规则：主 agent 分批生成

**用例生成必须由主 agent 直接完成**，按模块分批生成，每批一个模块。禁止使用子任务（subagent/Task工具）生成用例内容。

**原因**：子任务（尤其是 fast 模型）无法正确理解和执行检查点归并规则、模块划分逻辑等需要判断力的工作，会导致：
- 每条用例检查点名称都不同（检查点粒度失控）
- 输出格式不符合合并要求
- 各子任务之间风格不一致

#### 正确的分批生成流程

1. 按模块图顺序，**逐模块生成**（每次生成一个模块的完整用例）
2. 每个模块生成前，回顾 [core_writing_rules.md](references/core_writing_rules.md) 中的检查点归并规则
3. 每个模块生成后，立即检查检查点粒度（2-10条/检查点）
4. 所有模块完成后，合并、重编号、写入

#### 子任务的允许用途

子任务**仅允许**用于以下机械性工作，**禁止**用于用例内容生成：
- 格式转换（JSON对象 → 二维数组）
- 数据合并与重编号
- 文件读写操作
- Google Sheets API 调用

#### 备选方案：子任务生成（仅当主 agent 明确无法完成时）

如果因上下文长度等客观限制，主 agent 确实无法一次完成某个模块的生成，可使用子任务作为**备选方案**，但必须：

1. 子任务 prompt 开头必须写：`请先读取 references/subagent_rules_compact.md 获取完整编写规则，严格遵循其中的检查点归并规则和输出格式。`
2. 输出格式必须为带合并的二维数组：`[["编号","模块","检查点","操作步骤","预期结果","备注"], ...]`
3. 禁止在 prompt 中写"一条用例只验证一个检查点"
4. 主 agent 收到结果后必须逐条检查检查点粒度，不合格的必须重新归并

### 完成后自检

用例写入完成后，自动检查以下情况：

| 检查项 | 触发条件 |
|--------|----------|
| **涉及原有功能** | PRD 关联已有系统（如英雄、战斗、商城等）但用例未覆盖相关配置 |
| **PRD 提及配置表** | PRD 中提到了配置表 ID 但用例未验证对应配置项 |
| **🔴 必选模块缺失** | 复杂系统的必选模块（派兵/战斗/战报/红点/多入口）中有模块为 0 条 |
| **🔴 场景清单未覆盖** | 战斗/派兵场景清单中「✅」的场景在用例中无对应 |
| **🔴 矩阵未覆盖** | 状态/条件矩阵中有行在用例中无对应 |
| **🔴 配置数值写死** | 预期结果中存在配置可调的具体数值（价格/奖励内容/数量/概率等），应改为"与配置一致" |
| **🔴 配置表标注缺失** | 涉及数值/概率/时间/奖励的用例备注中缺少配置表ID+字段名 |
| **🔴 配置值误拆模块** | 将配置数值的特殊值（如$0、100%、1次）解读为独立功能模块（如"首次免费"、"必得"），PRD未明确描述该机制时应归入对应流程 |

**如果检测到以上情况**，本次内容校验未通过：已确认范围内且有依据可修正的当轮补齐并复查；缺必要输入的报告部分完成及具体缺口，继续澄清。不得把原任务的必要补齐变成“是否需要补充”的可选动作。

```
用例部分完成：缺少 <具体输入>，尚未覆盖 <已确认场景>。
已完成内容位于 <文件或在线链接>；写入成功不代表覆盖验收通过。
```

**如果未检测到问题**，只提示完成：

```
用例已完成（共 xxx 条）：已确认规则及适用场景覆盖通过，文件回读核验通过。产物：<实际链接>。
```

---

## 功能类型策略

| 类型 | 场景 | 策略文件 | 用例数参考 |
|:----:|------|----------|:----------:|
| A | 新系统/新玩法 | [type_a_newfeature.md](references/type_a_newfeature.md) | 100-1500条 |
| B | 功能优化/迭代 | [type_b_update.md](references/type_b_update.md) | 50-150条 |
| **C** | 礼包/商业化 | [type_c_iap.md](references/type_c_iap.md) | 50-180条 |
| **D** | 运营活动 | [type_d_activity.md](references/type_d_activity.md) | 200-500条 |

**通用场景模板**：[common_scenarios.md](references/common_scenarios.md)（⚠️ 只读「模板索引」+「模板选用指南」→ 根据功能类型跳转读取匹配模板，不要全文读取）

---

## 游戏知识库触发规则

> **阅读 PRD 后自动检测关键词，匹配则必须阅读对应知识库**
>
> **⚠️ 读取知识库后，必须查看其「必验场景/必验维度清单」章节，并在生成前输出「本 PRD 涉及的必验项」**

### 通用 SLG 知识库

| 关键词 | 触发知识库 | 文档 | 含必验清单 |
|--------|------------|------|:----------:|
| 英雄、斗士、主将、副将、星级、品质 | 英雄系统 | [hero_system.md](references/game_knowledge/hero_system.md) | ✅ |
| 战斗、出征、攻击、驻防、技能、buff | 战斗系统 | [battle_system.md](references/game_knowledge/battle_system.md) | ✅ |
| 红点、提示点、入口提示 | 红点系统 | [redpoint_system.md](references/game_knowledge/redpoint_system.md) | ✅ |
| KVK、跨服、军团、氏族、野战区 | 跨服系统 | [cross_server_system.md](references/game_knowledge/cross_server_system.md) | ✅ |

### 项目特有知识库

> 以下知识库来自项目配置层 `testcase-config/game_knowledge/`，由各项目自行维护。
> AI 在读取通用知识库后，还需检查 `testcase-config/project_config.md` 中的「关键词触发规则」，读取项目特有的知识文件。

| 对话、剧情、NPC、章节任务、叙事、文案、dialogue、chapter_quest、fte_npc | X2 叙事设计规范（辅助需求分析） | [x2-narrative-design/SKILL.md](../x2-narrative-design/SKILL.md) | |

**知识库读取后必须执行**：
1. 查看知识库末尾的「必验场景/必验维度清单」
2. 标注本 PRD 涉及哪些必验项
3. 将涉及的必验项纳入「模块图」或「生成前检查清单」
4. 生成后自检：必验项是否全部覆盖

---

## 用例数量估算

| 功能类型 | 人工用例参考 | AI工作量参考 |
|----------|:--------:|:------:|
| 运营活动(D) | 250-350 | 200条 |
| 新系统(A)简单 | 300-500 | 250条 |
| 新系统(A)复杂 | 800-1500 | 600条 |
| 新系统(A+)大型 | 1200-1800 | 1000条 |
| 小游戏玩法(D+) | 800-1000 | 700条 |

> 模块级别用例数标准见 [core_writing_rules.md](references/core_writing_rules.md)

---

## 输出格式

### JSON 格式

```json
[
  ["DK2_001", "排行榜", "入口", "检查活动主界面", "显示排行榜入口", ""],
  ["DK2_002", "", "", "", "入口图标符合需求", ""],
  ["DK2_003", "", "", "点击排行榜入口", "正确进入排行榜界面", ""]
]
```

### 字段说明

| 列 | 字段 | 规则 |
|----|------|------|
| A | 编号 | 必填，格式：`前缀_三位数字` |
| B | 功能模块 | 同模块第一条填写，后续写 `""` → 自动合并 |
| C | 检查点 | 同检查点第一条填写，后续写 `""` → 自动合并 |
| D | 操作步骤 | 同操作第一条填写，后续写 `""` → 自动合并 |
| E | 预期结果 | **必填**，每条用例必须有预期结果 |
| F | 备注 | 可选：配置表ID、本地化、⚠️待确认 |

---

## 参考文档

| 类型 | 文档 |
|------|------|
| **⭐核心写法规则** | [core_writing_rules.md](references/core_writing_rules.md)（模块/检查点划分、四大原则、配置数值写法等） |
| **⭐场景验证模式** | [scenario_patterns.md](references/scenario_patterns.md)（62种AI常漏验证模式，⚠️ 只读索引+触发章节） |
| **⭐执行检查清单** | [execution_checklist.md](references/execution_checklist.md)（生成前/后自检流程、测试方法论） |
| **工作流程** | [需求确认](references/0_requirement_confirm.md) · [PRD分析](references/1_analysis.md) |
| **通用场景模板** | [common_scenarios.md](references/common_scenarios.md)（⚠️ 只读索引+选用指南→按需跳转匹配模板） |
| **文件操作** | [file-operations-compact.md](references/file-operations-compact.md) |
| **版本历史** | [CHANGELOG.md](CHANGELOG.md) |
| **叙事设计参考（剧情/对话类功能辅助）** | [x2-narrative-design/SKILL.md](../x2-narrative-design/SKILL.md) |

### 人工用例样本

> 人工用例样本存放在各项目的 `testcase-config/doc/` 目录中，请从项目配置层获取。

---

## ⚠️ 验收反馈转化机制

> **用户的验收反馈是最有价值的实战经验，必须100%转化为可执行规则！**

| 反馈类型 | 转化目标 | 写入位置 |
|----------|----------|----------|
| **遗漏了某类用例** | 新增场景模板+触发关键词 | core_writing_rules.md |
| **写法格式错误** | 新增写法规范示例 | core_writing_rules.md |
| **配置表标注缺失** | 强化配置标注规则 | core_writing_rules.md |

### 历史反馈记录

| 日期 | 反馈要点 | 转化规则 | 状态 |
|------|----------|----------|:----:|
| 2026-01 | 推送通知只写触发不写消失/位置/重复 | 推送6维度验证规范 | ✅ |
| 2026-01 | 配置数值写死导致维护困难 | 配置数值写法规范（用n代指） | ✅ |
| 2026-01 | 任务进度只写状态不写达成条件逻辑 | 任务4维度验证规范 | ✅ |

> **每次验收后必须更新此表！**
